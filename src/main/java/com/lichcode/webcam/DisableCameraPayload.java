package com.lichcode.webcam;

import net.minecraft.util.Identifier;

import static com.lichcode.webcam.WebcamMod.MOD_ID;

public class DisableCameraPayload {
    public static final Identifier DISABLE_CAMERA_PAYLOAD_ID = Identifier.of(MOD_ID, "disable_camera");
}
